# RAG 문서 분할을 위한 필수 패키지
# 기본 패키지들은 Python 표준 라이브러리에 포함되어 있음

# 선택적 패키지 (고급 기능 사용시)
# langchain>=0.1.0  # LangChain 통합시
# sentence-transformers>=2.0.0  # 임베딩 생성시
# faiss-cpu>=1.7.0  # 벡터 검색시
# numpy>=1.21.0  # 수치 연산시
# pandas>=1.3.0  # 데이터 분석시

# 개발 및 테스트용
jupyter>=1.0.0
notebook>=6.4.0
